<% provide(:title, t("views.users.index.tul")) %>
<h1><%= t"views.users.index.tul" %></h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
</ul>

<%= will_paginate %>
